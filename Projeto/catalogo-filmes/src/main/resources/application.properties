spring.application.name=catalogo-filmes
server.port=8080

# Banco H2 em memória (os dados são apagados quando a aplicação é encerrada)
spring.datasource.url=jdbc:h2:mem:catalogo;DB_CLOSE_DELAY=-1
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.open-in-view=false

# Integração com o OMDb
omdb.url=https://www.omdbapi.com/
omdb.api-key=68b46e45
